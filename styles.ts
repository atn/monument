import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  cell: {
    backgroundColor: '#fff',
    borderRadius: 9,
    padding: 11,
    margin: 4,
  },
  bottom: {
    flex: 1,
    justifyContent: 'flex-end',
    marginBottom: 36,
    padding: 20
  }
})